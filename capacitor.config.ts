import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cheetahx.bharathdevelopment',
  appName: 'cheetah x',
  webDir: 'build'
};

export default config;
